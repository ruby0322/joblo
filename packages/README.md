# Packages workspace

This directory is reserved for shared reusable packages in the monorepo.

Recommended structure:

- `packages/<package-name>/src/`
- `packages/<package-name>/tests/`
- `packages/<package-name>/requirements.txt`
