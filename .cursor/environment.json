{
  "install": "python3 -c \"import sys; assert sys.version_info[:2] >= (3, 12), 'Python 3.12+ is required'\" && python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt",
  "start": "python3 --version"
}
