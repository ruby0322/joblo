# Services workspace

This directory is reserved for deployable applications in the monorepo.

Recommended structure:

- `services/<service-name>/src/`
- `services/<service-name>/tests/`
- `services/<service-name>/requirements.txt`
